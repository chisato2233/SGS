# Text normalization
* text=auto
.githooks/* text eol=lf

# Git LFS policy
#
# Do not blanket-track binary extensions. Files should only be placed in LFS
# when their individual working-tree size is at least 50 MiB.
#
# Run Tools/CheckLargeFiles.ps1 before committing. When a staged file crosses
# the threshold, run the same script with -Fix to add an explicit per-file LFS
# rule below.

# Explicit large-file LFS entries generated by Tools/CheckLargeFiles.ps1:
